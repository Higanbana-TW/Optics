{
  "name": "optic-player",
  "private": true,
  "version": "1.0.0",
  "type": "module",
  "scripts": {
    "dev": "vite --host 0.0.0.0",
    "build": "vite build",
    "preview": "vite preview --host 0.0.0.0",
    "test": "node --test src/mipi-raw/convert.test.js",
    "test:raw": "python3 -m unittest -v test_mipi_raw_convert.py",
    "build:standalone": "node scripts/build-standalone-raw.mjs"
  },
  "dependencies": {
    "hls.js": "^1.6.17"
  },
  "devDependencies": {
    "vite": "^8.2.0"
  }
}
